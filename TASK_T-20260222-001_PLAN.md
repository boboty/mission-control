# 任务 T-20260222-001 实现计划

## 目标
推进浮窗详情内容增强 + 数据结构优化

## 需求拆解

### 1. 浮窗详情字段完善
- [x] 元信息 (Metadata): ID, 创建时间，更新时间，来源等
- [ ] 关联对象 (Related Objects): 依赖任务，子任务，关联事件等
- [ ] 时间线 (Timeline): 状态变更历史，重要事件记录
- [ ] 操作入口 (Action Entries): 快速操作按钮（编辑，完成，删除等）

### 2. 数据结构可扩展性
- [ ] 优化 DetailData 接口，支持嵌套数据
- [ ] 添加 timeline 和 relatedObjects 字段
- [ ] 添加 actions 配置支持
- [ ] 保持向后兼容，不破坏现有卡片

### 3. 默认方案（产品决策）
**关联对象**: 
- tasks: 显示 depends_on 指向的任务
- pipelines: 显示关联的 items
- events: 显示参与者/关联任务

**时间线**:
- 显示状态变更（从 API 获取或计算）
- 显示关键时间点（创建、更新、完成）

**操作入口**:
- 编辑（预留）
- 标记完成（任务）
- 删除（预留）
- 复制链接

## 实现步骤

1. 更新 DetailData 接口定义
2. 增强 DetailModal 组件
3. 更新数据转换函数
4. 添加 API 支持（如需要）
5. 测试并截图
6. Build 验证
7. Git commit

## 验收标准
- [ ] npm run build 通过
- [ ] 至少 1 张关键 UI 截图
- [ ] 关键 API 响应样例
- [ ] 本地 commit（不 push），提交信息带任务号
